# State Capacity TinyRNN

Clean reconstruction of the TinyRNN state-capacity study under the revised full-research protocol.

This repository is intentionally stricter than the earlier exploratory implementation. Previous outputs are treated as audit material only. No empirical claim is accepted as final unless it is regenerated by scripts in this folder and appears in `outputs/audit/claim_audit.tsv`.

## Source of Truth

- Protocol: `docs/codex_full_research_manual_state_capacity.md`
- Revised manuscript framing: `docs/state_capacity_revised_intro_methods_NHB.docx`

## Current Status

Step 00 is implemented first:

```bash
make audit_existing
```

The full pipeline is not yet allowed to produce a manuscript until the ANN intervention gate, external validations, master statistics table, figures, claim audit, and final decision report exist.

For a plain-language explanation of every protocol step, see:

- `docs/step_pack.md`
